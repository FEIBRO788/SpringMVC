# ?????
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/empdb?serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root

mybatis:
  mapper-locations: classpath:mappers/*.xml #??mapper????????
  configuration:
    map-underscore-to-camel-case: true #??????

logging:
  level:
    cn.tedu.springmvc: debug #??cn.tedu.baking???????